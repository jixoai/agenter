// AUTO-GENERATED FILE. DO NOT EDIT.
// Run `bun run build:skills` from packages/app-server to rebuild.

import type { RuntimeBuiltinSkillCatalogEntry } from "../runtime-skill-catalog-builder";

export const runtimeBuiltinSkillCatalog = [
  {
    "name": "agenter-attention",
    "summary": "Inspect and settle obligations. Use this when you need to understand or update attention debt.",
    "sourcePath": "packages/attention-system/skills/attention/SKILL.md",
    "packageName": "@agenter/attention-system",
    "packagePath": "packages/attention-system",
    "template": "---\nname: agenter-attention\ndescription: Inspect and settle obligations. Use this when you need to understand or update attention debt.\n---\n\n# agenter-attention\n\nUse this skill when you need to inspect unresolved obligations or record that an obligation has truly progressed.\n\nQuick start:\n1. If the current task already names the concrete file, room, terminal, or URL work to do next, do that real work before browsing attention.\n2. Run `attention list` only when you need the active `contextId` or need to confirm which obligations still remain.\n3. Run `attention query` with JSON `stdin` only if `attention list` is still not enough.\n4. When the real work is finished, run `attention commit` with JSON `stdin`.\n\nKey laws:\n- `score > 0` means the obligation still exists.\n- `done: true` is the normal way to resolve the active scores for a context.\n- Lower scores only after the real work has actually happened.\n- For `attention query` and `attention commit` through `root_bash`, default to JSON `stdin`; only use argv JSON when the payload is trivially short.\n- If `attention query --help` or `attention commit --help` marks compact as `Suggested` or `Available`, `--compact` is an optional positional mode. If the positional array becomes unclear, switch back to standard object JSON immediately.\n- For a simple single-room delivery task, do not interrupt the first action just to inspect attention if the current room already states the work clearly.\n- If you already sent the required durable reply, verified the file, or confirmed the external side effect, settle the attention in the same round instead of leaving solved work active.\n- If the work is still waiting on another participant, relay room, or external source, the obligation is not complete yet and must stay unresolved.\n- For relay work, `done: true` becomes legal only after the origin room already received the final answer, not merely an acknowledgement that you are asking someone else.\n\nCommon endings:\n- Delivery finished and the user already got the result:\n  `attention commit '{\"contextId\":\"ctx-...\",\"summary\":\"Delivered the required reply and verified the result.\",\"done\":true}'`\n- You need to keep the work open because evidence is still missing:\n  leave the context unresolved, gather evidence, and only commit once the real state changed.\n\nReferences:\n- `references/settlement.md`: how to decide whether attention is still active and how to use `commit`\n"
  },
  {
    "name": "agenter-collaboration",
    "summary": "Coordinate in shared rooms without breaking role boundaries or repeating already-claimed ownership. Use this when multiple users or avatars share one room.",
    "sourcePath": "packages/app-server/skills/collaboration/SKILL.md",
    "packageName": "@agenter/app-server",
    "packagePath": "packages/app-server",
    "template": "---\nname: agenter-collaboration\ndescription: Coordinate in shared rooms without breaking role boundaries or repeating already-claimed ownership. Use this when multiple users or avatars share one room.\n---\n\n# agenter-collaboration\n\nUse this skill when you are collaborating in a shared room such as a project room.\n\nQuick start:\n1. Read the latest room messages.\n2. Identify who owns the missing fact or decision.\n3. Use the exact room protocol or prefix required by that room.\n4. If you already own the next artifact or contract, leave the room and do that private work next.\n5. Return with one protocol-shaped room reply only when shared truth changed.\n6. If a prior room message is invalid, send a corrected replacement.\n\nKey laws:\n- Room messages are durable shared truth.\n- Stay inside your role boundary.\n- Keep a single source of truth for shared contracts and ownership.\n- Do not rewrite artifact ownership toward your own specialty when another role is the better fit.\n- Claim ownership once. Do not repost the same \"I will handle X\" after the room already knows.\n- Once ownership is clear, the usual next move is workspace or terminal work, not another acknowledgement in the room.\n- Another participant's acknowledgement is not by itself a new obligation for you.\n- Re-enter the room when shared truth actually changed: you need a narrow dependency, you must correct a shared fact, you are handing off an artifact or contract, or you are delivering the final result.\n- A room message that claims a local URL, API, or \"ready\" state is shared truth, not a plan. Do not post that claim until the exact promised URL or path already works now.\n- Private reminders are coordination hints, not shared truth; they usually mean a shared-room obligation is still open.\n- If the room is waiting on a final protocol reply, terminal success alone is not enough; post the required shared-room message before you consider the work finished.\n\nReferences:\n- `references/shared-room-protocols.md`: room protocol usage, ownership boundaries, and correction patterns\n"
  },
  {
    "name": "agenter-message",
    "summary": "Read, query, send, edit, and recall durable room messages. Use this when work depends on room context, room history must be searched, a room expects a protocol-shaped reply, or a chat-related attention item still needs a user-visible reply.",
    "sourcePath": "packages/message-system/skills/message/SKILL.md",
    "packageName": "@agenter/message-system",
    "packagePath": "packages/message-system",
    "template": "---\nname: agenter-message\ndescription: Read, query, send, edit, and recall durable room messages. Use this when work depends on room context, room history must be searched, a room expects a protocol-shaped reply, or a chat-related attention item still needs a user-visible reply.\n---\n\n# agenter-message\n\nUse this skill when you need to read or query room history, send, edit, or recall a durable room message, or decide whether a chat-related attention item still requires a room reply.\n\nQuick start:\n1. If the latest room message already contains the full task and no other room is involved yet, do not reread the room first.\n2. If the task already shows the exact room `chatId`, use that literal `chatId` directly for `message send`, `message edit`, `message recall`, or `message read`.\n3. Run `message list` once only when you truly need a `chatId` or need to discover another visible room.\n4. Run `message read` with JSON `stdin` only when room history may actually change the decision.\n5. Run `message query` when you need cross-message lookup, fielded search, or guarded SQL over the rooms you already hold.\n6. For temporary cross-room search, prefer `chatId:\"*\"` so the runtime resolves only the rooms you already have grants for.\n7. Decide whether the room needs an acknowledgement, a narrow follow-up question, or a final reply.\n8. Send one durable message with the correct room scope and protocol. If you are replying to a specific earlier room message, include `ref`. Through `root_bash`, prefer `command=message send` plus JSON `stdin`; only use argv JSON for trivial single-line payloads. If `message send --help` marks compact as `Suggested` or `Available`, `message send --compact` is also available for positional payloads.\n9. `message send` returns `recentMessages`. Inspect them immediately as a lightweight post-send sanity check.\n10. If two of your recent messages look similar, run `message read` before changing history so you can also see `referencedItems` and the nearby reply context.\n11. Similar wording alone is not enough to justify recall. Judge the room's actual conversation context first.\n12. If the repetition was intentional in the room's protocol or request, keep both messages.\n13. If your prior room message is invalid or stale and you already know its `messageId`, prefer `message edit` to correct that durable reply in place.\n14. If the earlier durable reply should disappear rather than remain visible with corrected wording, use `message recall` on that `messageId` before sending a replacement.\n15. If you cannot safely edit or recall the earlier durable reply, send a corrected replacement.\n16. If that reply completes the obligation, switch to `attention` and settle the same context.\n\nKey laws:\n- The origin room owns the user-visible conversation.\n- For `message read`, `message query`, `message send`, `message edit`, and `message recall` through `root_bash`, default to JSON `stdin`; only use argv JSON when the payload is trivially small.\n- If `message read --help`, `message query --help`, `message send --help`, `message edit --help`, or `message recall --help` marks compact as `Suggested` or `Available`, `--compact` is an optional positional mode. If the positional array becomes unclear, switch back to standard object JSON immediately.\n- Use `ref` when the new room message is replying to a specific earlier durable message.\n- `message send` returns `recentMessages`; treat them as a quick self-check, not a replacement for a full room read.\n- `message read` returns the visible `items` plus one-hop `referencedItems`, so you can inspect nearby reply context before deciding whether to revise room history.\n- If the task already gives the exact room `chatId`, that literal room id is enough to send the acknowledgement or final reply; do not rediscover the same room through `message list`.\n- `message query` is message-history lookup, not a generic workspace search tool.\n- `message query` only sees the room scope you already hold. `chatId:\"*\"` means \"all rooms currently granted to me\", not \"all rooms in the system\".\n- Use `mode:\"match\"` for literal text lookup, `mode:\"query\"` for structured message filters, and `mode:\"sql\"` only when the analysis genuinely needs aggregation or projection.\n- `mode:\"sql\"` is read-only and runs on a pre-authorized temporary projection. If the runtime rejects your SQL, tighten the query instead of trying to mutate the dataset.\n- If you already have the prior durable reply's `messageId`, `message edit` is the cleanest correction path for your own earlier message.\n- If the earlier message should no longer remain visible in the room, `message recall` is the cleanest withdrawal path for your own earlier message.\n- If you do not have the `messageId`, or the earlier message belongs to someone else, send a corrected follow-up message instead of guessing.\n- Prefer `message edit` when the same room fact should stay visible with corrected content; prefer `message recall` when the prior room fact should be withdrawn before you post a replacement.\n- Similar wording alone is not enough to justify recall; decide from the actual room context and whether the repetition was intentional in the room's protocol or request.\n- If the current room message already fixes the task, URL, or required reply token, the normal next move is acknowledgement or tool work, not another room read.\n- If the room context already exposes `visibleRooms`, those rooms are real durable channels you can use now; a participant missing from the origin room is not automatically unavailable.\n- If the user asks you to ask or relay to another participant and a matching visible room already exists, relay there instead of telling the user that the participant is not here.\n- If you relay out from the origin room, \"I'm asking them now\" is only an acknowledgement, not the final delivery.\n- Room messages are durable shared truth.\n- Chat attention is not finished just because tools succeeded; it finishes when the necessary room reply has been sent.\n- If the room will wait through multi-step work, send one short acknowledgement before the deeper tool work starts.\n- A good acknowledgement is brief: usually one or two short sentences, not a full requirement recap or numbered execution plan.\n- Reply promptly when the room is waiting on you, but do not spam the room with every internal step.\n- While another participant or room still owes you the missing fact, keep the origin-room obligation unresolved; do not close it with `attention commit ... done=true` yet.\n- After the relay target replies, the next required move is to deliver that answer back into the origin room before settling the attention.\n- If the current attention says `self_update` / `no_external_reply_needed`, that usually means you should continue private work or settle the attention, not send another room reply yet.\n- Private reminders do not replace the required durable room reply; they usually mean the shared-room obligation is still open.\n- Follow the room's requested protocol exactly.\n- If the room already fixed a URL, path, or other reply token, preserve that fact exactly instead of silently normalizing it.\n- Terminal success alone is not the room reply. Once the exact promised URL or path has been freshly verified, send the required room message before you treat the chat work as done.\n- After the required room reply has been sent, the usual next move is `attention list` followed by `attention commit ... done=true`.\n- A later feedback message reopens the room obligation; do not settle that new debt until the new room reply has actually been sent.\n\nReferences:\n- `references/room-protocols.md`: room ownership, protocol prefixes, and correction behavior\n- `references/chat-attention-items.md`: acknowledgement timing, chat etiquette, and settling chat-related attention items\n"
  },
  {
    "name": "agenter-runtime",
    "summary": "Orient yourself inside the runtime shell. Use this first when you need to choose between root tools, shell CLI, and deeper skill docs.",
    "sourcePath": "packages/app-server/skills/runtime/SKILL.md",
    "packageName": "@agenter/app-server",
    "packagePath": "packages/app-server",
    "template": "---\nname: agenter-runtime\ndescription: Orient yourself inside the runtime shell. Use this first when you need to choose between root tools, shell CLI, and deeper skill docs.\n---\n\n# agenter-runtime\n\nUse this skill when you need to understand the runtime surface before choosing a more specific system skill.\n\nQuick start:\n\n1. Run `workspace_list`.\n2. If the task already names an exact room, path, URL, file, or terminal target, make one real command for that target before browsing deeper docs.\n3. Run `skill list` only when you truly need a more specific skill family.\n4. Run `skill info <skill>` only after the direct command path is still unclear.\n5. Run `skill get-config <skill>` when you need to inspect which files define that skill's live watcher truth.\n6. If a shell command rejects your arguments, run `<command> --help`.\n\nKey laws:\n\n- The only direct model tools are `workspace_list`, `root_bash`, and `workspace_bash`.\n- `workspace_list` shows mounted project workspaces as `{ id, cwd, alias }`; root stays special and is reached through `root_bash`.\n- `root_bash` is a one-shot shell for root control-plane work, not a durable process owner.\n- `workspace_bash` is a pure mounted-workspace shell selected by `workspaceId`; it does not carry the root runtime-local control plane.\n- `root_bash` also accepts optional `stdin`; for runtime-local CLI commands that take JSON payloads, default to `command=<bare action>` plus JSON `stdin`.\n- Use a single argv JSON payload only when it is trivially short, single-line, and clearly cheaper in tokens than opening a separate `stdin` field.\n- If `<command> --help` marks compact as `Suggested` or `Available`, `--compact` is also available as an optional positional array mode. If the array shape becomes unclear, fall back immediately to standard object JSON.\n- `root_bash` can use outbound network access for objective verification of current or external facts.\n- Long-lived or interactive work belongs in `terminal`.\n- A local delivery URL may be verified from `root_bash`, but the process that owns that URL still belongs in `terminal`.\n- A `terminal read` snapshot or a still-running process is not enough to prove a local delivery URL is ready; use a fresh exact-path HTTP check from the one-shot shell.\n- When a room or user names a concrete local URL, scheme, host, port, and path are all part of the delivery contract.\n- If the latest room message already contains the full task, do not pause the first real action just to reread the room or browse `SKILL.md`.\n- `skill info` returns the real filesystem path to the skill's `SKILL.md`.\n- `skill get-config` returns the skill config path plus the resolved watched files; `skill set-config` replaces that config JSON when you need to retarget skill live sync.\n- Runtime-owned helper scripts are optional. Only inspect `~/tools` when `root_bash` or the shell shows they actually exist.\n- Helper scripts must not replace core work such as writing required workspace files or owning application network behavior.\n\nReferences:\n\n- `references/discovery.md`: progressive skill discovery and how to expand one skill at a time\n- `references/shell-surface.md`: root tools, one-shot bash semantics, and bash vs terminal boundaries\n- `references/toolbox.md`: optional runtime helper scripts under `~/tools` and the boundary for using them\n"
  },
  {
    "name": "agenter-terminal",
    "summary": "Create, recover, and drive durable terminal sessions. Use this when work needs a long-lived or interactive process.",
    "sourcePath": "packages/terminal-system/skills/terminal/SKILL.md",
    "packageName": "@agenter/terminal-system",
    "packagePath": "packages/terminal-system",
    "template": "---\nname: agenter-terminal\ndescription: Create, recover, and drive durable terminal sessions. Use this when work needs a long-lived or interactive process.\n---\n\n# agenter-terminal\n\nUse this skill when work needs a durable process, an interactive shell, or recoverable terminal context.\n\nQuick start:\n1. Run `terminal list` when you need to recover or reuse an existing process owner.\n2. Run `terminal create` when no suitable terminal exists yet.\n3. Run `terminal write`.\n4. Run `terminal read` only to inspect or recover terminal state.\n\nKey laws:\n- A runtime does not start with a terminal by default.\n- Long-lived and interactive work belongs in terminals, not one-shot bash.\n- If work needs a port listener, local web server, watch mode, REPL, or retryable boot sequence, start it in `terminal`.\n- `terminal write` means the input was delivered, not that the process succeeded.\n- After starting a listener in `terminal`, inspect its real state and verify the exact promised URL or path before you tell a room or user that it is ready.\n- `terminal read` snapshots and \"the process is still running\" only describe terminal state; they do not prove the promised URL or API path actually responds.\n- When the task already names the workspace and delivery target, the normal next move is to create or recover the terminal, not to browse unrelated room or attention detail first.\n- If a one-shot shell hits binding or sandbox errors while you are trying to make a service reachable, stop and switch to `terminal`.\n- When more than one workspace is mounted, choose an explicit absolute `cwd`.\n- `terminal create`, `terminal write`, and `terminal read` are JSON-first commands. Through `root_bash`, default to `command=<bare terminal action>` plus JSON `stdin`.\n- Only use a single argv JSON payload when it is trivially short and clearly cheaper in tokens than a separate `stdin` field.\n- If `terminal create --help`, `terminal write --help`, or `terminal read --help` marks compact as `Suggested` or `Available`, the matching command also accepts `--compact` positional arrays. If the positional array becomes unclear, go straight back to standard object JSON.\n\nReferences:\n- `references/terminal-lifecycle.md`: create/list/read/write/kill strategy and recovery patterns\n- `references/file-writing.md`: safe patterns for sending multi-line file writes through terminal input\n"
  },
  {
    "name": "agenter-workspace",
    "summary": "Inspect mounted real paths and choose the correct absolute cwd. Use this when path authority is unclear.",
    "sourcePath": "packages/app-server/skills/workspace/SKILL.md",
    "packageName": "@agenter/app-server",
    "packagePath": "packages/app-server",
    "template": "---\nname: agenter-workspace\ndescription: Inspect mounted real paths and choose the correct absolute cwd. Use this when path authority is unclear.\n---\n\n# agenter-workspace\n\nUse this skill when you need to know which real paths are mounted and which absolute path should own the work.\n\nQuick start:\n1. Run `workspace list`.\n2. Inspect mounted workspace paths and grant modes.\n3. Choose an explicit absolute `cwd` when starting terminal work.\n\nKey laws:\n- The avatar root workspace is always mounted.\n- Project access still depends on explicit workspace mounts and grants.\n- Real filesystem paths are the source of truth.\n\nReferences:\n- `references/mounts-and-cwd.md`: how to read mounts and choose the correct absolute working directory\n"
  }
] as const satisfies readonly RuntimeBuiltinSkillCatalogEntry[];
