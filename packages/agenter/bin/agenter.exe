echo "Error: agenter native binary not installed." >&2
echo "" >&2
echo "Either postinstall did not run (--ignore-scripts) or the platform-native" >&2
echo "optional dependency was not downloaded (--omit=optional)." >&2
echo "" >&2
echo "Run the postinstall manually (adjust the path for local vs global install):" >&2
echo "  node node_modules/agenter/install.cjs" >&2
echo "" >&2
echo "Fallback runtime path:" >&2
echo "  node node_modules/agenter/cli-wrapper.cjs" >&2
exit 1
