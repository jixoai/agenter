{
  "scripts": {
    "test:tools": "bun run src/test-tool-agents.ts",
    "test:recall": "bun run src/test-recall-orchestrator.ts",
    "test": "bun run test:tools && bun run test:recall",
    "server": "bun run src/api-ws.ts"
  },
  "dependencies": {
    "@tanstack/ai": "^0.3.0",
    "chromadb": "^3.2.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^25.1.0"
  }
}