# Changesets

This directory stores release intent records.

- Create a changeset with: `bun run changeset`
- Bump versions with: `bun run version-packages`
- Publish (when enabled) with: `bun run release-packages`
