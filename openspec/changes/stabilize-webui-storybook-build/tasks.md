## 1. Toolchain Contract

- [x] 1.1 Write the Storybook static-build stabilization proposal, design, and capability spec.
- [x] 1.2 Define the implementation checklist for the WebUI Storybook dependency upgrade and verification.

## 2. Implementation

- [x] 2.1 Upgrade the `@agenter/webui` Storybook dependency line to the compatible upstream patch release and refresh the lockfile.
- [x] 2.2 Remove temporary diagnostics, then verify `storybook:build` and the focused Storybook DOM regression suite against the upgraded toolchain.
