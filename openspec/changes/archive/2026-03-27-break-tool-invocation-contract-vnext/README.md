# break-tool-invocation-contract-vnext

Breaking change that replaces legacy `tool_call`/`tool_result` message channels with one structured `tool` invocation contract across runtime persistence and Devtools rendering.
