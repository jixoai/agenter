## 1. OpenSpec contract

- [x] 1.1 Add the `runtime-skill-progressive-disclosure` spec covering concise `SKILL.md`, sibling references, and the global `ccski info -> real path -> references` discovery law
- [x] 1.2 Confirm the breaking boundary: built-in skills stop inlining cross-system delivery/network workaround details inside atomic skill overviews

## 2. Skill authoring refactor

- [x] 2.1 Rewrite the six built-in runtime skills into concise overviews with explicit references indexes
- [x] 2.2 Add sibling `references/*.md` files for the detailed material that still belongs to each skill's own boundary
- [x] 2.3 Update global system prompts and `skills.list` guidance so AI learns to use `ccski info` real paths plus on-demand `cat` of reference files

## 3. Validation

- [x] 3.1 Add or update unit tests for skill discoverability, references structure, and global prompt wording
- [x] 3.2 Run targeted backend validation plus real AI room-terminal regression to confirm the new concise skill structure still works in practice
