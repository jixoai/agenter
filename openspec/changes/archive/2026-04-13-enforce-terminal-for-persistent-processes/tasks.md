## 1. Contract

- [x] 1.1 Record the one-shot-shell vs terminal law in proposal/design/spec delta

## 2. Implementation

- [x] 2.1 Add a shared AST-based guard that rejects background statements in one-shot workspace bash execution
- [x] 2.2 Apply the guard to both root workspace bash and regular workspace bash
- [x] 2.3 Strengthen runtime skills and localized system prompts so AI treats terminals as the only durable host for long-lived processes

## 3. Validation

- [x] 3.1 Add regression coverage for background-process rejection on one-shot bash
- [x] 3.2 Re-run workspace system tests plus the real project-room collaboration scenario
