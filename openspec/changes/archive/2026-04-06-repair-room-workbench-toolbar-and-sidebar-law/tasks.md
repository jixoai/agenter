## 1. Shell And Spec Alignment

- [x] 1.1 Remove or relocate auth/bootstrap shell affordances that violate the three-destination primary navigation law
- [x] 1.2 Update shared shell/workbench wiring so the selected Messages room window renders as `chrome_tabs -> page_toolbar -> page_content`

## 2. Room Toolbar And Chat Stage Repair

- [x] 2.1 Refactor the Messages room toolbar into a fixed-height band that keeps avatar/title/actions/chips inside toolbar chrome on desktop
- [x] 2.2 Repair the shared room chat stage so only the transcript list scrolls and the composer stays pinned inside page content

## 3. Compact Verification

- [x] 3.1 Make room tabs and toolbar responsive on iPhone 14-sized viewports without transcript overlap
- [x] 3.2 Verify desktop and iPhone 14 walkthroughs, then archive the change
