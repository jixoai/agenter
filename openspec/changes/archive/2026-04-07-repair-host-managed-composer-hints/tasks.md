## 1. Shared Composer Contract

- [x] 1.1 Add OpenSpec deltas for host-managed composer hint ownership

## 2. Implementation

- [x] 2.1 Make `default-composer` render the host-provided hint text without transport-specific override
- [x] 2.2 Add focused `@agenter/web-chat-view` coverage for the host-managed hint contract

## 3. Verification

- [x] 3.1 Run targeted `@agenter/web-chat-view` / `@agenter/webui` typecheck and browser verification for the new-room composer hint
