## Why

We need the Flutter chat-view track to share the same channel protocol and plugin model as the web implementation without blocking the current delivery.

## What Changes

- Add the `flutter-chat-view` package placeholder.
- Freeze protocol and package-level design docs for future Flutter / Flutter Web implementation.
