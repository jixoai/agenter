## 1. Message Workbench Stability

- [ ] 1.1 Repair the `Messages` workbench mount path so initial room hydration no longer blanks the page
- [ ] 1.2 Keep the fixed `New room` tab and later room tabs renderable through the shared workbench chrome contract

## 2. Regression Coverage

- [ ] 2.1 Add focused regression coverage for the pre-hydration `Messages` workbench render path
- [ ] 2.2 Run targeted verification for the repaired `Messages` routes and shared WebUI type safety
